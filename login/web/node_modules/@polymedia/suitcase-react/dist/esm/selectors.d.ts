export type RadioOption<T extends string> = {
    value: T;
    label: React.ReactNode;
};
export type RadioSelectorProps<T extends string> = {
    options: RadioOption<T>[];
    selectedValue: T;
    onSelect: (value: T) => void;
    className?: string;
};
/**
 * A generic radio button menu.
 */
export declare function RadioSelector<T extends string>({ options, selectedValue, onSelect, className, }: RadioSelectorProps<T>): import("react/jsx-runtime").JSX.Element;
