import { SuiClient } from "@mysten/sui/client";
/**
 * Like `CoinMetadata` from `@mysten/sui`, but includes the coin `type`.
 */
export type CoinMeta = {
    type: string;
    symbol: string;
    decimals: number;
    name: string;
    description: string;
    id: string | null;
    iconUrl: string | null;
};
/**
 * Fetch coin metadata from the RPC and cache it.
 */
export declare class CoinMetaFetcher {
    protected readonly client: SuiClient;
    protected readonly cache: Map<string, CoinMeta | null>;
    constructor({ client, preloadUrl, preloadData, }: {
        client: SuiClient;
        preloadUrl?: string;
        preloadData?: CoinMeta[];
    });
    getCoinMeta(coinType: string): Promise<CoinMeta | null>;
    getCoinMetas(coinTypes: string[]): Promise<Map<string, CoinMeta | null>>;
}
