import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useEffect, useRef } from "react";
import { useClickOutside } from "./hooks";
import { IconClose } from "./icons";
export const Modal = ({ onClose, children, }) => {
    const modalContentRef = useRef(null);
    useClickOutside(modalContentRef, onClose);
    useEffect(() => {
        const handleEscapeKey = (event) => {
            if (event.key === "Escape") {
                onClose();
            }
        };
        document.addEventListener("keydown", handleEscapeKey);
        return () => {
            document.removeEventListener("keydown", handleEscapeKey);
        };
    }, [onClose]);
    return (_jsx("div", { className: "poly-modal-background", children: _jsxs("div", { className: "card poly-modal-content", ref: modalContentRef, children: [_jsx("div", { className: "poly-modal-scrollable-content", children: children }), _jsx(IconClose, { className: "poly-modal-close icon", onClick: onClose })] }) }));
};
//# sourceMappingURL=modals.js.map