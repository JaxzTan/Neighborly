/**
 * Get SUI from the faucet on localnet/devnet/testnet.
 */
export declare function requestSuiFromFaucet(network: "localnet" | "devnet" | "testnet", recipient: string): Promise<{
    status: "Success" | {
        Failure: {
            internal: string;
        };
    };
    coins_sent: {
        amount: number;
        id: string;
        transferTxDigest: string;
    }[] | null;
}>;
