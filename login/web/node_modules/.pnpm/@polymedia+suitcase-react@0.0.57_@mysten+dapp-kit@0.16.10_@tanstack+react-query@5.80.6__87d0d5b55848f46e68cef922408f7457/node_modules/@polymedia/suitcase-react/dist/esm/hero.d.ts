import { ReactNode } from "react";
export declare const HeroBanner: ({ title, subtitle, description, actions, extra, }: {
    title: string;
    subtitle?: ReactNode;
    description?: ReactNode;
    actions?: ReactNode;
    extra?: ReactNode;
}) => import("react/jsx-runtime").JSX.Element;
