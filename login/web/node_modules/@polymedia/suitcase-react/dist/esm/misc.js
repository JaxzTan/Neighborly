/**
 * Encode a URL for use in CSS `url()` syntax.
 */
export function makeCssUrl(url) {
    return "url("
        + encodeURI(url) // encode the URL
            .replace(/\(/g, "%28") // encode '('
            .replace(/\)/g, "%29") // encode ')'
        + ")";
}
//# sourceMappingURL=misc.js.map